fastapi>=0.110
uvicorn[standard]>=0.27
requests>=2.31
pydantic>=2.6
python-dotenv>=1.0
# Stance classifier for the live path (training and inference)
scikit-learn>=1.4
scipy>=1.11
numpy>=1.26
joblib>=1.3
# Optional: LLM for claim extraction on the live path. Without it, the curated DB is used.
anthropic>=0.39
# Server-side transcript fetching (auto-generated captions too, when run locally). 1.x API.
youtube-transcript-api>=1.0
